# 学习通自动学习助手 - 线程安全修复报告

## 问题描述

程序在运行时出现以下错误：
```
NameError: cannot access free variable 'e' where it is not associated with a value in enclosing scope
greenlet.error: cannot switch to a different thread (which happens to have exited)
```

## 根本原因

在 Tkinter 应用中，所有 UI 操作必须在主线程执行。但程序的多个函数在后台线程（Playwright 网络操作线程）中直接操作 UI 组件或在 lambda 闭包中引用局部变量，导致：

1. **跨线程 UI 操作** - 后台线程直接调用 `configure()`、`set()` 等 UI 方法
2. **Lambda 闭包陷阱** - 延迟执行的 lambda 引用外部变量 `e`，但变量在 lambda 执行时已不在作用域内

## 修复方案

### 1. 统一使用 `self.root.after(0, callback)` 模式

所有在后台线程中的 UI 操作都通过 `after()` 调度到主线程执行。

### 2. Lambda 表达式立即捕获变量

对于需要在 lambda 中使用的外部变量，使用默认参数立即捕获其值：

**错误写法：**
```python
self.root.after(0, lambda: messagebox.showerror("错误", f"失败：{str(e)}"))
```

**正确写法：**
```python
error_msg = str(e)
self.root.after(0, lambda msg=error_msg: messagebox.showerror("错误", f"失败：{msg}"))
```

## 修复的具体位置

### ✅ 第 294 行 - start_browser() 函数
```python
# 修复前
self.status_var.set("等待用户登录中...")

# 修复后
self.root.after(0, lambda: self.status_var.set("等待用户登录中..."))
```

### ✅ 第 300 行 - start_browser() 登录成功
```python
self.root.after(0, lambda: self.status_var.set("已登录"))
```

### ✅ 第 306 行 - start_browser() 登录超时
```python
self.root.after(0, lambda: self.status_var.set("登录超时"))
```

### ✅ 第 310-312 行 - start_browser() 异常处理
```python
# 修复前
self.root.after(0, lambda: messagebox.showerror("错误", f"启动失败：{str(e)}"))

# 修复后
error_msg = str(e)
self.root.after(0, lambda msg=error_msg: messagebox.showerror("错误", f"启动失败：{msg}"))
```

### ✅ 第 325 行 - fetch_courses() 函数
```python
# 修复前
self.status_var.set("获取课程中...")

# 修复后
self.root.after(0, lambda: self.status_var.set("获取课程中..."))
```

### ✅ 第 435-440 行 - fetch_courses() 更新 UI
```python
# 修复前
self.root.after(0, self._update_course_list)
self.start_btn.configure(state=tk.NORMAL)
self.status_var.set(f"已加载 {len(self.courses)} 门课程")

# 修复后
def update_ui():
    self._update_course_list()
    self.start_btn.configure(state=tk.NORMAL)
    self.status_var.set(f"已加载 {len(self.courses)} 门课程")

self.root.after(0, update_ui)
```

### ✅ 第 447-451 行 - fetch_courses() 异常处理
```python
# 修复前
self.root.after(0, lambda: messagebox.showerror("错误", f"获取课程失败：{str(e)}..."))

# 修复后
error_msg = str(e)
self.root.after(0, lambda msg=error_msg: messagebox.showerror("错误", f"获取课程失败：{msg}..."))
```

### ✅ 第 557-563 行 - _learn_course() 完成提示
```python
# 修复前
self.root.after(0, lambda: self.course_tree.set(tree_item, "status", "已完成"))
self.root.after(0, lambda: messagebox.showinfo("完成", f'课程"{self.current_course["name"]}"已学习完成'))

# 修复后
course_name = self.current_course["name"]
item = tree_item
self.root.after(0, lambda i=item: self.course_tree.set(i, "status", "已完成"))
self.root.after(0, lambda name=course_name: messagebox.showinfo("完成", f'课程"{name}"已学习完成'))
```

### ✅ 第 704-716 行 - _reset_buttons() 函数
```python
# 修复前
def _reset_buttons(self):
    self.start_btn.configure(state=tk.NORMAL)
    self.pause_btn.configure(state=tk.DISABLED, text="暂停")
    self.stop_btn.configure(state=tk.DISABLED)
    self.status_var.set("就绪")

# 修复后
def _reset_buttons(self):
    def _reset():
        self.start_btn.configure(state=tk.NORMAL)
        self.pause_btn.configure(state=tk.DISABLED, text="暂停")
        self.stop_btn.configure(state=tk.DISABLED)
        self.status_var.set("就绪")
    
    try:
        self.root.after(0, _reset)
    except:
        _reset()  # 如果已经在主线程，直接执行
```

## 验证结果

✅ Python 语法检查通过  
✅ 程序可以正常启动  
✅ 不再出现 `greenlet.error` 错误  
✅ UI 操作流畅，无线程冲突

## 技术要点总结

### Tkinter 线程安全的三个原则

1. **所有 UI 操作必须在主线程**
   - 使用 `root.after(0, callback)` 从后台线程调度到主线程

2. **Lambda 闭包要立即捕获变量**
   - 使用默认参数：`lambda x=value: use(x)`
   - 而不是直接引用：`lambda: use(value)`

3. **长时间操作放在后台线程**
   - Playwright 网络操作、sleep 等耗时操作应在 `threading.Thread` 中执行
   - 避免阻塞主线程导致界面冻结

## 修复日期

2026-09-02

---

**开发者：** 大学在读生 丁辉  
**版权所有 © 2026 丁辉。保留所有权利。**
