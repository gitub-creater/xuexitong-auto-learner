# 学习通自动学习助手 - 更新日志

## 2026-09-03 v1.2 - 修复学习进度不动 + AI 答题接口

### 🐛 问题修复

**修复了"点开始学习后学习进度一点不动"的核心问题**

**根因：** 学习通的章节列表、视频播放器、题目弹窗全部嵌套在**多层 iframe** 里，旧代码所有 DOM 操作（找章节、找视频、答题）都在主页面顶层文档上查找，什么也找不到，进度自然一动不动。

**修复内容：**

1. ✅ **学习流程整体重写（基于 Playwright frame 遍历）**
   - 在所有层 iframe 中递归查找视频，不假设固定层级
   - 让官方播放器真实播放，进度由官方播放器自行上报（合规、不绕过风控）
   - 浏览器启动参数增加 `--autoplay-policy=no-user-gesture-required`，允许程序触发播放
   - 一节内有多个视频时逐个播完

2. ✅ **章节推进改为官方"下一节"按钮**
   - 视频播完自动点击页面上的官方"下一节"按钮（优先使用官方功能）
   - 点击后自动处理确认弹窗
   - 找不到"下一节"即视为整门课学习完成
   - 进入第一章失败时，留有 60 秒人工兜底：用户在浏览器里手动点开任意一节，程序自动接管继续刷

3. ✅ **弹题自动作答重写**
   - 覆盖 radio/checkbox 输入框型和 div 列表型两种弹题界面
   - 检测到弹题 → 选选项 → 提交 → 关闭结果弹层 → 恢复播放，全程自动

4. ✅ **"获取课程列表"按钮不再默认禁用**（v1.1 引入的误禁用）
   - 登录判断改为检测页面是否已离开登录页（学习通登录后落地 URL 不固定，不能依赖 `**/space/**`）

### 🎉 新功能

**AI 答题设置（可选，默认关闭）**

- 点击主界面"AI 答题设置"按钮配置
- **接口兼容 OpenAI 格式，支持中转站**：自定义接口地址（`/chat/completions`）+ 模型名 + API Key，智谱 / 方舟 / 各类中转站均可接入
- 未启用时使用本地策略：自动选择一个选项提交，让视频继续播放（弹题是互动打卡性质，不影响视频进度）
- 启用后程序把题干和选项发给所填接口挑选答案；调用失败自动回退本地策略
- API Key 仅保存在程序目录 `config.json`，不上传、不与学习通账号关联

### ✅ 验证结果

- 语法检查通过；9 段内嵌 JS 全部通过语法验证
- 离线冒烟测试通过：本地答题策略 / AI 回退逻辑 / 配置读写 / 中转站请求构造与响应解析
- 真实刷课效果需登录学习通账号实测

---

## 2026-09-02 v1.1 - 线程安全修复

### 🐛 问题修复

**修复了严重的线程安全问题（Tkinter 跨线程异常）**

**问题描述：**
程序在后台线程中直接操作 UI 组件，导致以下错误：
```
NameError: cannot access free variable 'e' where it is not associated with a value in enclosing scope
greenlet.error: cannot switch to a different thread (which happens to have exited)
```

**修复内容：**

1. ✅ **start_browser 函数**
   - 所有 `self.status_var.set()` 调用改为通过 `self.root.after(0, lambda: ...)` 调度到主线程

2. ✅ **fetch_courses 函数**
   - `self.status_var.set()` 改为线程安全调用
   - 所有 UI 更新操作统一封装到 `update_ui()` 函数，通过 `after()` 调度

3. ✅ **_reset_buttons 函数**
   - 封装所有按钮状态重置逻辑
   - 自动判断当前线程，安全调度到主线程

4. ✅ **字符串语法错误修复**
   - 修复了提示信息中引号嵌套导致的 SyntaxError

### 🔧 技术改进

- 统一使用 `self.root.after(0, callback)` 模式进行跨线程 UI 更新
- 确保所有 Tkinter 组件操作都在主线程执行
- 保持 Playwright 网络操作在后台线程，不阻塞 UI

### ✅ 验证结果

- 程序可以正常启动
- 不再出现线程相关错误
- UI 响应流畅，不会冻结

---

## 2026-09-02 v1.0 - 首次发布

### 🎉 核心功能

- ✅ 自动播放学习通视频
- ✅ 智能识别并处理题目弹窗
- ✅ 连续学习多个章节，自动跳转
- ✅ 暂停/继续/停止控制
- ✅ 精美的深色主题界面
- ✅ 完整的开发者信息和版权声明

### 📖 使用说明

请参考 README.md 文档。

---

**开发者：** 大学在读生 丁辉  
**版权所有 © 2026 丁辉。保留所有权利。**
